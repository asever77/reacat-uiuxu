# 🚀 APP ROUTER 디렉토리 구조

app router에서는 layout.tsx는 상속이 되는 구조  
layout > template > page

- layout.tsx : 여러 페이지 간에 공유되는 UI (헤더, 사이드바 등). 상태가 유지됩니다.
- template.tsx : Layout과 비슷하지만, 경로 이동 시마다 새 인스턴스를 생성 (상태 초기화).

- page.tsx : 해당 경로의 유니크한 UI. 실제 렌더링되는 페이지 콘텐츠입니다.
- loading.tsx : 해당 세그먼트의 콘텐츠가 로드되는 동안 보여줄 로딩 UI (React Suspense).
- default.tsx : Parallel Routes(병렬 라우트) 사용 시, 매칭되지 않을 때 보여줄 기본 UI.

- not-found.tsx : notFound() 함수가 호출되거나 일치하는 경로가 없을 때 보여줄 UI (404).
- error.tsx : 런타임 에러 발생 시 보여줄 에러 UI (React Error Boundary).
- global-error.tsx : 루트 레이아웃(app/layout.tsx)의 에러를 잡는 전역 에러 핸들러.

- route.ts : UI가 아닌 API 엔드포인트 (GET, POST 등)를 정의.
  - 예: app/users/route.ts와 app/users/page.tsx는 같이 쓸 수 없습니다. API는 보통 app/api/users/route.ts 처럼 별도 경로로 분리하는 것이 좋습니다.

```
📂.storybook
│  ├──📄main.ts
│  ├──📄preview.ts
│  └──📄vitest.setup.ts
📂public
│  ├──📂images
│  └──📄mockServiceWorker.js
📂src
├──📂app
│  ├──📂(error)
│  │  ├──📄error.tsx
│  │  ├──📄global-error.tsx  # Root error
│  │  ├──📄layout.tsx  # Root > error Layout
│  │  └──📄not-found.tsx  # 404 페이지
│  │
│  ├──📂(page)
│  │  ├──📄layout.tsx  # Root > page Layout
│  │  ├──📂(main)
│  │  │  ├──📄layout.tsx  # Root > page > main Layout
│  │  │  └──📄page.tsx
│  │  │
│  │  ├──📂(sub1)
│  │  │  ├──📂sub1-1
│  │  │  │  ├──📂_components
│  │  │  │  │  ├──📄InteractiveButton.tsx
│  │  │  │  │  └──📄InnerComponent.tsx
│  │  │  │  ├──📂@modal
│  │  │  │  │  ├──📂modal-name1
│  │  │  │  │  │  └──📄page.tsx
│  │  │  │  │  ├──📂modal-name2
│  │  │  │  │  │  └──📄page.tsx
│  │  │  │  │  └──📄default.tsx  # @modal 안의 페이지가 없는 경우 설정
│  │  │  │  ├──📄layout.tsx  # Root > page > sub1-1 Layout
│  │  │  │  └──📄page.tsx
│  │  │  │
│  │  │  └──📂(sub1-2)
│  │  ├──📂(sub2)
│  │  │  ├──📂(sub2-1)
│  │  │  └──📂(sub2-2)
│  │  └──📂(sub3)
│  │
│  ├──⭐favicon.tsx
│  ├──🎨globals.css
│  └──📄layout.tsx  # Root Layout
│
├──📂components  # 공통 컴포넌트
│  ├──📂contents
│  │  ├──📄Typo.tsx  # 컨텐츠 컴포넌트
│  │  └──📄index.ts  # 배럴 파일
│  │
│  ├──📂icons
│  │  ├──📄CommonIcons.tsx  # SVG 아이콘 컴포넌트
│  │  └──📄index.ts  # 배럴 파일
│  │
│  ├──📂layout
│  │  ├──📄Footer.tsx
│  │  └──📄Header.tsx
│  │
│  ├──📂ui  # shadcn 설치된 파일 최종 사용 안함
│  └──📂uiux  # shadcn 수정한 파일로 최종 사용 할 컴포넌트
│     ├──📄accordion.tsx  # shadcn-custom 컴포넌트
│     └──📄index.ts  # 배럴 파일
│
├──📂mocks  # mock 데이터
│  ├──📄userHandlers.ts
│  └──📄index.ts  # 배럴 파일
│
├──📂providers  # mock 데이터
│  └──📄StoreProvider.tsx
│
├──📂stores
│  ├──📂hooks
│  │  └──📄index.ts
│  └──📂slice
│     ├──📄{name}Slice.ts
│     ├──📄index.ts
│     └──📄index.ts
│
└──📂stories  # stroybook 페이지
```
